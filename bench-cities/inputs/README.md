# Save-input dumps go here.
#
# Press "Zapisz wsad" / "Save input" in the City Builder and drop the file in
# this folder. Unlike a copied report it carries building coordinates and the
# road tiles of the live city, so the whole run reproduces - including the
# baseline phases that measure and polish the city you already have.
